Your AEM project doesn't have any published content yet — the site at `aem.live` and `aem.page` returns 404 for all pages, including the homepage, navigation, sitemap, and query index. This is a fresh boilerplate with no authored content.Could you share the sitemap URL? Please paste the full URL (e.g., `https://example.com/sitemap.xml`) and I'll discover all pages and layouts from it.
